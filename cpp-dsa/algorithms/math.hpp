#include <vector>


/**
 * Returns the least common multiple of a vector of integers.
 * 
 * @param vals The vector of integers.
 * @return The least common multiple of the vector of integers.
*/
int leastCommonMultiple(std::vector<int> vals);
